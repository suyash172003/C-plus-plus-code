#include <iostream>
using namespace std;
int main(){
    int t;
    while(t-->0){
        int x,y;
        cin>>x>>y;
        string s1,s2;
        s1=to_string(x);
        s2=to_string(y);
        for(int i=s1.length()-1;i>=0;i--){
            swap(s1[i],s2[i]);
            min=Math.min(s1[i])
        }
    }
}